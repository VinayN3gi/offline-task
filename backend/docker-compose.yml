services:

  db:
    image: postgres:15
    container_name: postgres_container
    restart: always
    environment:  # Fixed typo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: test123
      POSTGRES_DB: mydb
    ports:
      - "5432:5432"  # Corrected format